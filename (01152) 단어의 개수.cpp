#include <bits/stdc++.h>
#define FASTIO                                                                 \
    ios_base::sync_with_stdio(0);                                              \
    cin.tie(0);

using namespace std;

int main() {
    FASTIO;

    string input;
    int cnt = 0;
    while (cin >> input) {
        cnt++;
    }

    cout << cnt;

    return 0;
}